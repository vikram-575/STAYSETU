rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections for authenticated users and platform operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
